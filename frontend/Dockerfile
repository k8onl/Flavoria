# Use the official, lightweight Nginx image
FROM nginx:alpine

# Copy all the frontend files into Nginx's public serving folder
COPY . /usr/share/nginx/html

# Expose port 80 for standard web traffic
EXPOSE 80